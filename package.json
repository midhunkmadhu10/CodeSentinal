{
  "name": "codesentinal-lite",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "backend": "cd backend && uvicorn main:app --reload --port 8000"
  },
  "workspaces": ["frontend"]
}